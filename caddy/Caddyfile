# HTTP to HTTPS redirect (automatic with Caddy, but explicit for clarity)

# Use localhost with automatic self-signed certificates
localhost {

    # backend
    handle /api/* {
        reverse_proxy api:3000
    }

    # frontend
    handle {
        reverse_proxy frontend:80
    }
}
